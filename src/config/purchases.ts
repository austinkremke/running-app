/** RevenueCat entitlement identifier for Run Off Pro — must match the entitlement
 * configured in the RevenueCat dashboard exactly, not a store product ID. */
export const PRO_ENTITLEMENT_ID = 'Run Off Pro';

/** RevenueCat package identifiers for the Run Off Pro offering's packages —
 * must match the package identifiers configured in the RevenueCat dashboard. */
export const PRO_PACKAGE_IDS = {
  monthly: 'monthly',
  yearly: 'yearly',
} as const;
