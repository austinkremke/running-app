/** Subtle drop shadow for cards that sit on top of another card/surface (e.g. inside MeScreen's scrollCard). */
export const cardShadow = {
  shadowColor: '#000',
  shadowOffset: { width: 0, height: 3 },
  shadowOpacity: 0.3,
  shadowRadius: 6,
  elevation: 4,
} as const;
